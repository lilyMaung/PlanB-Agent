{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/plan/status", "destination": "/api/plan/status" },
    { "source": "/api/plan", "destination": "/api/plan" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}